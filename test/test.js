class Rectangle {
  constructor(height, width) {
    this.height = height;
    this.width = width;
  }
};

const hello = new Rectangle(40, 40);
console.log(typeof hello);
console.log(typeof Rectangle);
